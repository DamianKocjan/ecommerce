import React from "react";
import { cva } from "class-variance-authority";
import type { VariantProps } from "class-variance-authority";

const {{camelCase name}} = cva("", {
	variants: {},
	defaultVariants: {},
});

export type {{properCase name}}Props = VariantProps<typeof {{camelCase name}}> & {
	className?: string;
};

export const {{properCase name}}: React.FC<{{properCase name}}Props> = ({ className, ...props }) => (
	<div className={{append '' '{'}}{{camelCase name}}({ class: className }){{append '' '}'}} {...props} />
);
